<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Toolbar>
        <Title id="headerTitle" text="{i18n>empManagementPortal}" level="H1"/>
        <ToolbarSpacer/>
            <ToggleButton
            id="languageToggle"
            text="{i18n>toggleLanguage}"
            press="onToggleLanguage"/>
    </Toolbar>
</mvc:View>
